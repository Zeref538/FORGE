# Force LF in the repo regardless of what Windows checks out with. CRLF has
# previously broken the portfolio site's indexer (wrong chunk counts).
* text=auto eol=lf

# Binary -- never touch line endings
*.onnx binary
*.pt binary
*.png binary
*.jpg binary
*.zip binary
